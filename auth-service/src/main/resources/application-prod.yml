spring:
  security:
	oauth2:
	  resourceserver:
		jwt:
		  issuer-uri: ${KEYCLOAK_ISSUER_URI}

eureka:
  client:
	service-url:
	  defaultZone: ${EUREKA_DEFAULT_ZONE:http://discovery-service:8761/eureka/}

